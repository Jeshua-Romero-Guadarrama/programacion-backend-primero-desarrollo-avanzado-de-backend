<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="utf-8" />
  <title>{{title}}</title>
  <meta name="viewport" content="width=device-width, initial-scale=1" />

  <!-- Bootstrap 5 -->
  <link
    href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css"
    rel="stylesheet"
    integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH"
    crossorigin="anonymous"
  />

  <style>
    .navbar-brand { font-weight: 700; letter-spacing: .2px; }
    .page-title { margin-bottom: .25rem; }
    .subtle { color: #6c757d; }
    footer { color:#6c757d; }
  </style>
</head>
<body class="bg-light">

  <nav class="navbar navbar-expand-lg navbar-dark bg-dark">
    <div class="container">
      <a class="navbar-brand" href="/">Tienda API</a>
      <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navMain">
        <span class="navbar-toggler-icon"></span>
      </button>
      <div id="navMain" class="collapse navbar-collapse">
        <ul class="navbar-nav me-auto">
          <li class="nav-item"><a class="nav-link" href="/home">Home</a></li>
          <li class="nav-item"><a class="nav-link" href="/realtimeproducts">Productos en tiempo real</a></li>
        </ul>
        <span class="navbar-text subtle">Node + Express + Handlebars + Socket.io</span>
      </div>
    </div>
  </nav>

  <main class="container py-4">
    <header class="mb-3">
      <h1 class="page-title">{{heading}}</h1>
      {{#if subtitle}}<p class="subtle mb-0">{{subtitle}}</p>{{/if}}
    </header>

    <!-- Mensajes (opcionalmente usados por realtime.js) -->
    <div id="flash" class="mb-3"></div>

    {{{body}}}
  </main>

  <footer class="container pb-4">
    <hr>
    <small>© {{year}} Tienda API • Demo académica</small>
  </footer>

  <!-- Bootstrap JS -->
  <script
    src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"
    integrity="sha384-YvpcrYf0tY3lHB60NNkmXc5s9fDVZLESaAA55NDzOxhy9GkcIdslK1eN7N6jIeHz"
    crossorigin="anonymous">
  </script>
</body>
</html>
