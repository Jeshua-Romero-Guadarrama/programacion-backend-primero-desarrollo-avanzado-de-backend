{{!-- src/views/home.handlebars --}}
<section class="card shadow-sm">
  <div class="card-header d-flex align-items-center justify-content-between">
    <span class="fw-semibold">Productos (HTTP)</span>
    <small class="text-muted">Esta vista se actualiza al recargar</small>
  </div>
  <div class="card-body p-0">
    {{#if products.length}}
      <div class="table-responsive">
        <table class="table table-hover align-middle mb-0">
          <thead class="table-light">
            <tr>
              <th>Título</th>
              <th>Código</th>
              <th class="text-end">Precio</th>
              <th class="text-end">Stock</th>
              <th class="text-muted">ID</th>
            </tr>
          </thead>
          <tbody>
            {{#each products}}
              <tr>
                <td class="fw-semibold">{{this.title}}</td>
                <td><span class="badge text-bg-secondary">{{this.code}}</span></td>
                <td class="text-end">{{currency this.price}}</td>
                <td class="text-end"><span class="badge text-bg-info">{{this.stock}}</span></td>
                <td class="text-muted">{{this.id}}</td>
              </tr>
            {{/each}}
          </tbody>
        </table>
      </div>
    {{else}}
      <div class="p-4">
        <div class="alert alert-warning mb-0">No hay productos cargados.</div>
      </div>
    {{/if}}
  </div>
</section>

<div class="mt-3">
  <small class="text-muted">
    Al crear/editar/eliminar por <code>/api/products</code>, recarga esta página para ver los cambios.
  </small>
</div>
